# 端点与资源地址均为可选覆盖；未设置时应用以“能力未配置”运行，不做任何官方域回退。
# 按需复制到 .env；部署覆盖也可放在未跟踪的 .env.local 中。
ZCODE_BASE_URL=

# 可选本地 Provider 配置文件路径；留空时使用内置第三方模板配置。
ZCODE_BUILTIN_PROVIDER_CONFIG_FILE=

# 内网或私有部署地址没有默认值，按需显式填写。
ZCODE_DEPS_BASE_URL=
INTRANET_MACHINE_HOST=
ZCODE_DIST_BASE_URL=
